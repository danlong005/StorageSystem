<h1>Bins</h1>

<a href="/auth/signout">Sign Out</a>
<br>

<a href="/users/<%= authentication.id %>/bins/new">Create Bin</a>

<ul>
    <% bins.forEach((bin) => { %>
        <li><a href="bins/<%= bin.id %>"><%= bin.name %></a></li>
    <% }); %>
</ul>
